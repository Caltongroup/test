# Sample logs

Drop the station's sample log files here, e.g.:

    KQBL MONDAY SAMPLE LOG.LOG
    KQBL TUESDAY SAMPLE LOG.LOG
    ... one per weekday ...

Then run `python -m musiclog analyze` (see the root README) to learn
the hour clock. The weekday is detected from the filename.
